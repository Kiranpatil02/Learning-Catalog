### Archiving
- Data which needs to beb compressed to to save space

#### Gzip
- Used to compress one or more files, it replaces the original file with a compressed version of the original file.
```
gzip  <file>
```
```
gunzip <file>
```
We can uncompress this file using gunzip command.